services:
  mersal-guard:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8090:8090"
    volumes:
      - mersal-data:/var/lib/mersal-guard
    env_file:
      - ../mersal-guard.env
    restart: unless-stopped

volumes:
  mersal-data:
